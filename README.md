##Api client library for elastic email

###Usage:
Register `ElasticEmailRequestBuilder` as a service.
Register `ElasticApiClient` as a service and inject your API key and Guzzle Http Client with Elastic API base uri set.